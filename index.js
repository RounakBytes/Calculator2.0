const add = (x, y) => {
  return x + y;
};

const substract = (x, y) => {
  return x - y;
};

const multiply = (x, y) => {
  return x * y;
};

console.log(add(5, 10));
console.log(substract(10, 5));
console.log(multiply(5, 25));
console.log(add(5, 10));
